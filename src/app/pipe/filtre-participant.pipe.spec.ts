import { FiltreParticipantPipe } from './filtre-participant.pipe';

describe('FiltreParticipantPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltreParticipantPipe();
    expect(pipe).toBeTruthy();
  });
});
