import { EvenementPipe } from './filtre-evenement.pipe';

describe('EvenementPipe', () => {
  it('create an instance', () => {
    const pipe = new EvenementPipe();
    expect(pipe).toBeTruthy();
  });
});
